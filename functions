function put_config {
    source=$1
    destination=$2

    cp $source $destination
    cat config|grep -v "^#"|while read key value; do
        sed -i -E "s/%%$key%%/$value/g" $destination
    done
}

function config_has {
    OPTION=$1
    grep -E "^$OPTION\$" config &> /dev/null
    return $?
}
